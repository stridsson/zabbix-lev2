[defaults]
inventory = inventory
ansible_managed = Ansible managed: modified on %Y-%m-%d %H:%M:%S
remote_user = devops
host_key_checking = False
private_key_file = /home/student/.ssh/id_lab

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
